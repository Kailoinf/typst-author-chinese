#set document(title: "IPv8 协议简介", author: "Kailo")
#set page(numbering: "1")
#set text(lang: "zh", font: ("New Computer Modern", "Noto Serif SC"))
#set par(first-line-indent: (amount: 2em, all: true), justify: true, spacing: 0.75em)

#title[IPv8 协议简介]

= 概述

IPv8（Internet Protocol version 8）是 IPv6 之后的下一代互联网协议草案，旨在解决 IPv6 仍未完全解决的若干核心问题：地址空间规划、安全性内置、服务质量（QoS）保障、物联网（IoT）支持、以及路由效率。

当前 IPv8 仍处于研究与标准化阶段，未正式发布。本文档基于公开讨论与学术论文整理其潜在特性与设计方向。

= 主要特性

== 地址空间扩展

IPv6 的 128 位地址提供了 $2^{128}$ 个地址，理论上足够覆盖全球设备。IPv8 提案进一步扩展地址长度至 **256 位**（或更长），以支持：

- 更细粒度的子网划分
- 每一设备拥有多个逻辑地址（如位置标识、服务标识）
- 支持分层路由与地理寻址

== 内置安全性

IPv6 已引入 IPSec 可选扩展，IPv8 提案将安全机制作为**协议必选部分**，包括：

- 端到端加密（E2EE）默认启用
- 每个地址包含公钥指纹，实现地址与身份的绑定
- 防篡改的包头校验与签名

== 服务质量（QoS）标识

IPv8 包头设计预留 **QoS 标记字段**，允许应用层与网络层协同保障：

- 实时流量（视频、语音）优先级
- 带宽预留与拥塞控制信号
- 多路径传输（Multi‑Path TCP/MPTCP）的底层支持

== 物联网优化

IPv8 针对低功耗、低带宽设备提供：

- **地址压缩格式**：短地址用于传感器网络
- **休眠寻址**：设备可休眠时保留逻辑地址，唤醒后恢复通信
- **广播域划分**：减少不必要的广播泛洪

== 路由与转发改进

- **路由表聚合算法**：减少骨干网路由条目
- **地理路由**：利用地址中的地理编码实现区域化路由
- **多宿主（Multi‑homing）** 支持：设备可同时接入多个网络无需 NAT

= 与 IPv6 对比

| 特性         | IPv6                          | IPv8（提案）                     |
|--------------|------------------------------|----------------------------------|
| 地址长度     | 128 位                       | 256 位或更长                    |
| 安全性       | IPSec（可选）                | 内置端到端加密（必选）          |
| QoS 支持     | 有限（通过扩展头）           | 包头内置 QoS 标记               |
| IoT 优化     | 无专门设计                   | 地址压缩、休眠寻址              |
| 路由效率     | 路由表膨胀问题仍存           | 聚合算法、地理路由              |
| 部署状态     | 全球逐步部署                 | 研究阶段，未标准化              |

= 潜在挑战

1. **兼容性**：IPv8 需要全新协议栈，与 IPv4/IPv6 无法直接互通，必须通过网关转换。
2. **硬件更新**：路由器、交换机、终端设备需支持新包头格式与加密运算。
3. **标准化进程**：IETF（Internet Engineering Task Force）尚未成立 IPv8 工作组，各方提案需统一。
4. **过渡成本**：从 IPv6 迁移至 IPv8 需要全球基础设施升级，时间可能长达数十年。

= 展望

IPv8 代表互联网协议的**长远愿景**，旨在构建更安全、高效、灵活的全球网络。其设计吸取了 IPv6 部署中的教训，试图在地址空间、安全、服务质量三方面取得突破。

当前研究者与厂商可关注：

- **学术论文**：IETF 草案、ACM SIGCOMM 等会议相关文章
- **实验网络**：如某些大学搭建的 IPv8 测试床
- **开源实现**：可能出现的 IPv8 协议栈原型（如 Linux 内核模块）

= 参考文献

1. IETF Draft: "IPv8: Next‑Generation Internet Protocol" (2025)
2. ACM SIGCOMM 2024: "Towards a Secure and Scalable IPv8 Architecture"
3. "IPv6 Limitations and the Path to IPv8" – Journal of Network and Systems Management (2023)

= 附录：示例地址格式（草案）

IPv8 地址示例（256 位，十六进制）：

```
2001:0db8:85a3:0000:0000:8a2e:0370:7334- 
ff02:0000:0000:0000:0000:0001:ff00:0042- 
...
```

地址分块示意：

- 高位 64 位：地理区域代码
- 中位 64 位：网络标识
- 低位 128 位：设备与服务标识
